# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Testerino
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pwd
      - run: ls -la
      - run: echo TESTERINO=secrets.TESTERINO
      - run: echo TESTERINO=${{ secrets.TESTERINO }}
      - run: touch einenTest.txt
      - run: ls -la
      - run: cat einenTest.txt
